export const initialState = {
  isLoading: false,
  urlId: '',
  previousUrl: '',
  error: null,
};

const reducer = (state, action) => {
  switch (action.type) {
    case '':
      break;

    default:
      return state;
  }
};

export default reducer;
